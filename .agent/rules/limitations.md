---
trigger: always_on
---

You are only allowed to work with these R modules:

library(apollo)
library(dplyr)
library(tidyr)


try to keep the number of papers 


For context always consider these pdfs : 
